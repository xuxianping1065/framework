package ${servicePackage};

import com.framework.common.service.BaseService;
import ${modelPackage}.${modelName};

/**
 *	${tableAlias}Service接口
 */
public interface ${serviceModel} extends BaseService<${modelName}>{

	
}
