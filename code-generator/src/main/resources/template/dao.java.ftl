package ${daoPackage};

import com.framework.common.dao.BaseDao;
import ${modelPackage}.${modelName};

/**
 *	${tableAlias}Dao
 */
public interface ${daoName} extends BaseDao<${modelName}>{
	
}